// src/components/Dashboard/Calendario.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const Calendario = () => {
  return (
    <Box>
      <Typography variant="h6" gutterBottom>
        Agenda
      </Typography>
      <Typography variant="body1">
        Visualize e agende sessões de treino, controle financeiro e emissão de recibos.
      </Typography>
    </Box>
  );
};

export default Calendario;
